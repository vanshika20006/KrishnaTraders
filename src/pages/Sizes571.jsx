
import Navbar from '../components/Navbar';
import React from 'react';
import Size11 from '../components/Size571';
/*
  Make sure the file name and import match exactly in case.
  If your file is named 'Size571.jsx', use:
  If your file is named 'size571.jsx', use:
  On some systems (like Windows), casing doesn't matter, but on others (like Linux or in some build tools), it does.
*/
  
  const VarientsPage = () => {
    return (
      <> 
        <Navbar />
       <Size11 />
      </>
    );
  };
  
  export default VarientsPage;